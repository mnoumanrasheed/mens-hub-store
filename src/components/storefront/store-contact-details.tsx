import { ExternalLink, Mail, MapPin, MessageCircle, Phone, Timer, Users } from "lucide-react";

type ContactSettings = { proprietors: string; phone: string; whatsapp: string; email: string; address: string | null; googleMapsUrl: string | null; storeTiming: string | null; instagramUrl: string | null; facebookUrl: string | null; tiktokUrl: string | null };
export function StoreContactDetails({ settings }: { settings: ContactSettings }) {
  const optional = Boolean(settings.address || settings.googleMapsUrl || settings.storeTiming || settings.instagramUrl || settings.facebookUrl || settings.tiktokUrl);
  return <div className="grid gap-4 text-sm text-muted"><Contact icon={<Users size={17} />} label={settings.proprietors} /><Contact href={`tel:${settings.phone.replace(/[^+\d]/g, "")}`} icon={<Phone size={17} />} label={settings.phone} /><Contact href={`https://wa.me/${settings.whatsapp}`} external icon={<MessageCircle size={17} />} label="WhatsApp" /><Contact href={`mailto:${settings.email}`} icon={<Mail size={17} />} label={settings.email} />{settings.address ? <Contact icon={<MapPin size={17} />} label={settings.address} /> : null}{settings.storeTiming ? <Contact icon={<Timer size={17} />} label={settings.storeTiming} /> : null}{settings.googleMapsUrl ? <Contact href={settings.googleMapsUrl} external icon={<ExternalLink size={17} />} label="View on Google Maps" /> : null}{optional ? <div className="flex flex-wrap gap-4">{settings.instagramUrl ? <Contact href={settings.instagramUrl} external label="Instagram" /> : null}{settings.facebookUrl ? <Contact href={settings.facebookUrl} external label="Facebook" /> : null}{settings.tiktokUrl ? <Contact href={settings.tiktokUrl} external label="TikTok" /> : null}</div> : null}</div>;
}
function Contact({ href, external, icon, label }: { href?: string; external?: boolean; icon?: React.ReactNode; label: string }) { const content = <>{icon}{label}</>; return href ? <a className="flex min-h-11 items-center gap-2 hover:text-gold" href={href} target={external ? "_blank" : undefined} rel={external ? "noopener noreferrer" : undefined}>{content}</a> : <p className="flex min-h-11 items-center gap-2 whitespace-pre-line">{content}</p>; }
