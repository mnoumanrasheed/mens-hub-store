# Data Access Layer

Server-only Prisma queries and minimal DTO projections belong here. Client Components must never import this directory or receive raw database records.

No data access is implemented during Phase 1.
